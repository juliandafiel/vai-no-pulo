// Re-export everything from react-native-maps for native platforms
export {
    default,
    Marker,
    Polyline,
    Polygon,
    Circle,
    Callout,
    Overlay,
    Heatmap,
    Geojson,
    PROVIDER_GOOGLE,
    PROVIDER_DEFAULT,
} from 'react-native-maps';

export type { MapPressEvent } from 'react-native-maps';
